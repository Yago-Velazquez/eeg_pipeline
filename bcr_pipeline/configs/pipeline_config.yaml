bcr:
  decision_threshold: 0.5
  decision_threshold_argmax_f1: 0.5
  decision_threshold_argmax_2rp: 0.5
  active_tag: hard_threshold_xgboost
  label_strategy: hard_threshold
  model_name: xgboost
  bad_threshold: 2
  n_splits: 5
  random_seed: 42

paths:
  data_csv: data/raw/Bad_channels_for_ML.csv
  results_dir: results
  figures_dir: results/figures
  models_dir: results
  configs_dir: configs
